---
scope: JazzTicketing platform — multi-tenant multi-region hospitality service operations; mobile + web console consuming Jazz Core
purpose: build-substrate
altitude: initiative
updated: 2026-09-04T15:30
---

- (direction) Fast path chosen by Tanim. Inputs are final: prd.md (83 FRs), DESIGN.md, EXPERIENCE.md, EXPERIENCE-WEB.md, and the two mockup sets. Nothing upstream is re-decided here.
- (constraint) Jazz Core is the sole upstream and is confirmed to provide all required capabilities and to be multi-region. No PMS/PBX code, credentials or certification enters JazzTicketing (PRD FR-49..57, 77, 78).
- (constraint) Multi-region at launch; a Tenant may span regions, a Property may not; region is immutable per Property (PRD DG-4, FR-83).
- (constraint) Eight locales including two RTL; Arabic ships in R1. Bidirectionality is a foundation requirement on both surfaces (PRD FR-61).
- (constraint) Property-issued shared handsets only, no BYOD path; Android 10 / iOS 15 / 3GB baseline (PRD NFR-5).
- (constraint) Existing team, Q2 2027 target for R1; R1 = tenancy + Job core + Jazz Core integration + mobile foundation + notifications + minimum reporting + guest request dispatch (PRD 6.3).
- (decision) SURVIVABILITY (NFR-2): cloud-only with a rich durable-offline mobile client. Tanim's call. No property-side hardware or agent. Binds every client and the Jazz Core edge. Prevents a second deployment target and an on-property support burden at every hotel. Consequence accepted: the console is unusable during a property WAN outage; mobile keeps working from its local store and queue.
- (decision) TENANCY (FR-1, FR-83): shared schema with tenant_id + property_id on every row, isolation enforced at the data-access boundary and proven by test, not by convention. Tanim's call. Prevents per-tenant schema drift and 40-way migrations. Consequence accepted: isolation is a code property, so it needs adversarial tests as a release gate rather than trust.
- (decision) Region topology: one full stack per region (regional cell). A Property is pinned to exactly one region's cell. Tenant-level records that must span regions (tenant identity, role definitions, corporate rollups) live in a global control plane holding NO guest-identifying data. Prevents cross-region guest data movement while still allowing FR-76 corporate views.
- (assumption) Stack recommended from training-data knowledge. Web verification is BLOCKED in this session, so every version below is UNVERIFIED and must be checked before anyone commits — the architecture skill requires web-confirmation of versions and that requirement is unmet by environment, not by choice.
- (event) Workspace bound; 10 activation entries logged.
- (decision) AD-1 event-sourced Job core: SLA is a fold over an ordered event log, never a stored countdown. Chosen because every hard requirement in this product is a time requirement (pause, escalate, survive reassignment, absorb an action taken offline 40 min ago). Buys the audit trail and the future AI training data for free.
- (decision) AD-2 occurred_at vs recorded_at split — the domain clock is when the actor acted, the system clock is when the server accepted it. Prevents a queued offline action silently rewriting SLA history and staff attribution.
- (decision) AD-13 one writing owner per aggregate, added by the adversarial pass: housekeeping completing a room and core/room owning status could both emit room-status events with different semantics while obeying every other AD.
- (decision) AD-14 one SLA fold in core/job called by everything, added by the adversarial pass: the worst hole found — dashboard and month-end report could each decide how to treat paused time and produce different compliance numbers, destroying SM-2 as a metric.
- (event) Reviewer gate: lint_spine.py clean after resolving frontmatter tokens; adversarial pass found 5 holes, closed by 2 new ADs and 2 tightenings. The version-verification reviewer could NOT run — web blocked — and that is stated in the spine rather than papered over.
- (event) Architecture spine distilled: 14 ADs, hexagonal + event-sourced Job core + regional cells, 10 consistency conventions, 3 diagrams, capability map over all 83 FRs, 8 deferred items.
- (event) spine finalized — status: final, 2026-09-02. Load-bearing calls were Tanim's: cloud-only with a rich durable-offline client (no property hardware), shared-schema tenancy with isolation as a tested code property, and a recommended stack whose versions are explicitly unverified.
- (event) bmad-create-epics-and-stories started; steps 1-2 written to planning-artifacts/epics.md. 10 epics (6 in R1), all 83 FRs claimed by exactly one epic, verified programmatically. Halted at the step-2 approval gate as the workflow requires before any story is written.
- (correction) Correction to the previous entry: the exactly-one-epic claim was FALSE when written. The verifier reported FR-4, FR-11, FR-14, FR-18 and FR-60 in two epics each and the document asserted exclusivity anyway. Resolved by giving each a single OWNING epic (FR-4 and FR-60 to E4, FR-11/FR-14/FR-18 to E3) and adding an explicit owner-vs-consumer table; E1 8 FRs, E5 4, E6 5. Re-verified: 83 owned, 0 missing, 0 duplicates.
- (decision by user) Mobile client changed from React Native to Flutter 3.2x + Dart 3.x (Tanim). Paradigm and all 14 ADs survive; the client edge moved. Consequences recorded: the 'TypeScript both sides' rationale is withdrawn, contracts/ promoted to schema of record with generated TS+Dart bindings and a CI drift gate, Drift-backed SQLite queue written in the same transaction as local state, AD-12 given concrete Flutter directional mechanisms, and AD-14 AMENDED to permit exactly one Dart port of the SLA fold fenced by cross-language fixture vectors as a release gate. lint_spine clean, 0 findings. Revision log section added to the spine.
- (event) Epics steps 3-4 complete: 85 stories across 10 epics written to planning-artifacts/epics.md with Given/When/Then ACs, FR citations and inherited AD/UX-DR criteria. Validated programmatically: 83/83 FR coverage from a generated coverage map, 0 within-epic forward dependencies, 4 declared cross-epic ones, 85/85 story-format compliance. Two findings raised for Tanim rather than resolved silently: no starter-template story exists although three CI release gates presuppose one (recommended Story 1.0), and Story 1.8 is oversized for one dev session. Halted at the step-4 [C] Complete gate.
- (decision by user) Tanim resolved both step-4 findings: (1) add Story 1.0 standing up the source tree with an inward-dependency lint rule, one reproducible region cell, and all three release gates green over trivial fixtures - plumbing with no FR, exception recorded on the story; (2) split old Story 1.8 into 1.8 Catalog Entries and SLA Targets (establishes the AD-9 versioning mechanism) and 1.9 Pause Conditions, Credits, Escalation chains and Inspection checklists (consumes it, adds no second one); old 1.9/1.10 shifted to 1.10/1.11. Final shape 10 epics, 87 stories. Re-validated: 83/83 FR coverage, 87/87 story format, 0 within-epic forward dependencies, 4 declared cross-epic ones.
- (event by user) bmad-create-epics-and-stories CLOSED by Tanim: epics.md status final, completed 2026-09-02. Final shape 10 epics / 87 stories / 83 FRs, all four workflow gates answered by the user (step-2 epic list, step-4 completion, plus the two step-4 findings). Downstream rules recorded in the document: it is not re-decided in implementation, the coverage map and dependency table stay generated rather than hand-maintained, the three CI gates precede all feature work via Story 1.0, and the stack versions remain unverified until 1.0 closes. Next run: bmad-create-story on Story 1.0.
- (event) bmad-create-story run on Story 1.0 (skill is deprecated in favour of bmad-build; invoked by name). Wrote implementation-artifacts/1-0-repository-cell-and-gates.md, status ready-for-dev, 7 tasks mapped to the 7 ACs transcribed verbatim from epics.md. Created sprint-status.yaml for Epic 1 only (no sprint-planning had run). Checklist pass added four implementation gaps under existing ACs rather than new criteria: a build-flagged fixture auth stub (the isolation suite needs a tenant session before 1.3/1.5 exist), a CI smoke test so 'the cell runs' is machine-checkable, a projection rebuild command, and an assertion that the control plane gains no guest-identifying column. Web research step could NOT run - blocked - so version confirmation is AC-7 work with a per-component check table instead of pinned numbers.
- (event by user) Tanim chose all 86 remaining stories at full depth (offered R1-deep/R2-R4-stub as recommended; declined). All 87 story files now exist in implementation-artifacts, ~125k words, mean 1,436 words each, every one ready-for-dev. Method: a generator transcribed each story statement and its ACs VERBATIM from the final epics.md so no criterion was retyped, then per-story tasks mapped to ACs, prerequisites, scope guards, implementation traps and testing were authored by hand, then one identical standing-constraints block (glossary, 14 ADs, layering, conventions, the four release gates, client UX criteria, unverified stack) was appended to all 86 so each file is self-contained for a dev agent that sees only it. sprint-status.yaml regenerated with all 87 keys. Verified programmatically: 87/87 have Story/AC/Tasks/DevNotes/References/DevAgentRecord/Prerequisites/scope-guard/Given blocks/source citations; all FR-1..83 cited; vocabulary check found and fixed one stray 'support ticket'. Caveat recorded for Tanim: every file was written before Story 1.0 is built, so none carries previous-story build learnings, and the 34 R2-R4 files will likely need a refresh pass when their release nears.
- (event) Story 1.0 IMPLEMENTED and delivered to the project repo (commit ecc3dd2, 78 source files, 29 tests passing). Verified against real Postgres 16.13 + Redis 7.0.15, then re-verified on Tanim's machine. Green: source tree + inward-dependency lint (29 modules, 0 violations, core/ forbidden from importing ANY npm package); contracts/ as schema of record with 4 generated bindings and a drift gate; cross-tenant isolation across all 5 public interfaces backed by Postgres RLS with per-transaction scope pinning, plus append-only events for the app role; one reproducible cell with migrations from source, guest-data-free control-plane assertion, smoke test, and a projection rebuild reproducing identical state; rebuilt from a clean clone with no manual step. Negative controls: 6 of 7 gates proven able to go red - and control #2 FOUND A REAL BUG in my drift gate (it regenerated before diffing, destroying the hand-edit it was meant to catch), now rewritten to snapshot content first. NOT done: the Dart half of AD-14's gate has never executed (Dart SDK download 403, pub.dev unreachable) so the gate is red by design, and T6's Flutter scaffold is blocked on the same. AC-7 found three real divergences: TypeScript current is 7.0.2 not 5.x (pinned 5.9.3, Tanim's call), NestJS is 12.0.1 not 10.x (framework decision deliberately deferred, ADR 0001, edge routes on node:http in ~40 lines), React is 19.2.8 not 18 (adopted). Story status: review.
- (event by user) Tanim asked whether the backend API and web app were dockerised. Honest answer: no - the repo had no Dockerfile, compose file or manifest, and AC-5 had been met with processes rather than images (the letter of 'reproducible from the repository alone' but weaker than the spine's 'Managed Kubernetes or equivalent per region'), and I had failed to raise it. Added at his request as T8 under AC-5 (no new AC; epics.md is final): API image (3-stage, prod deps only, USER node, HEALTHCHECK, exec CMD, graceful SIGTERM, and the SAME image runs migrations and projection rebuild so a deploy cannot apply migrations from a different build); console image (vite build behind non-root nginx on 8080, with tsc -b AND the directional lint inside the image build, and an entrypoint writing /config.json so ONE image serves every environment); docker-compose with healthchecks and service_completed_successfully ordering; secrets kept out of image layers via APP_DB_PASSWORD rotation in migrate.ts; scripts/gate-containers.mjs (static container gate that runs with or without a Docker CLI, parsing the compose YAML directly); scripts/compose-smoke.sh; CI container-images job. NOT VERIFIED: the images have never been built - every container registry is refused by egress policy so buildkit cannot resolve a base image, and base tags are therefore NOT digest-pinned (pin before deployment). Two new negative controls confirmed red (drop USER node; downgrade the migrate condition) - 8 of 9 now confirmed. The container work also found a LATENT Story 1.0 bug: the console annotated JSX.Element which React 19 removed, invisible because vite build does not type-check. Commit 231424e.
- (event by user) Ran both containers at Tanim's request. Every registry blocked, so: base images made build ARGs (real defaults) and the '# syntax=' directive removed (it forced a Docker Hub pull for an unused feature), then substitute bases built from the host filesystem via docker import. RUNNING THEM FOUND A REAL BUG: the API container crash-looped with MODULE_NOT_FOUND because Story 1.0 used @core/* tsconfig paths, which are compile-time only - tsc does not rewrite them - so node dist/edge/src/main.js had NEVER worked while all 29 tests passed, since vitest resolved the aliases itself and the smoke test imported from source. THE BUILT ARTIFACT HAD NEVER BEEN EXECUTED. Fixed with relative imports; added gate:built-artifact which runs node dist, asserts it serves health and drains on SIGTERM, needs no Docker, with a negative control. Second defect found the same way: health returned HTTP 500 on missing datastore config; now returns 200 degraded naming the unreachable dependency (a 500 from health tells the reader nothing). Final state: both containers healthy per Docker HEALTHCHECK, api 3001 non-root uid 1000 read-only rootfs, console 8081 non-root uid 101 serving shell + SPA fallback + immutable asset caching + /config.json from env; a command went through the container into Postgres and back via the projection; cross-tenant isolation held against the container (404 + error envelope); SIGTERM drained cleanly. 30 tests, 7 gates, 11 negative controls (10 red-verified). Commit 4ddce18.
- (decision by user) Tanim: add the auth API contract to contracts/openapi.yaml as part of Story 1.5.
- (decision) Designed the WHOLE auth surface (9 operations) rather than only 1.5's share: ownership stays with 1.3 (GET /auth/session, POST /auth/context), 1.5 (sso/start, sso/callback, token/refresh), 4.1 (device/sign-in, sign-out), 4.8 (GET+DELETE /auth/sessions). All nine share one Session and token shape; discovering that in 4.8 would mean changing what 1.3 already shipped, in Dart and TS at once. ADR docs/decisions/0002.
- (decision) Designed-ahead operations are kept honest by machinery, not discipline: x-story + x-implemented:false in the spec, edge/src/not-implemented.ts DERIVES its 501s from the document (before tenancy resolution, since 4 of 9 are how a credential is obtained), and the smoke suite asserts a documented operation NOT marked unbuilt is reachable - so flipping the flag is not a way to mark work done. Controls 14-17 prove each goes red.
- (finding) DEFECT (Story 1.0, fixed): ops/migrate.ts rotated the app role password with 'ALTER ROLE jt_app WITH PASSWORD $1'. Postgres takes no bind parameters in a utility statement. Compose and CI both leave APP_DB_PASSWORD empty so the branch never ran; it would have failed on the FIRST real deploy - the only environment where the secret store supplies a value. Now escaped via pg's escapeLiteral.
- (finding) DEFECT (Story 1.0, fixed): error codes 'conflict' and 'internal' had no message in en or ar - declared since 1.0, unnoticed because nothing had raised them, and would have rendered a BLANK label at the moment something had already gone wrong. Drift gate now fails on any error code missing a message in any language (AD-12).
- (finding) PRD GAPS flagged, not invented: (1) no token lifetimes - yet the access-token lifetime IS the deprovisioning delay FR-3 promises, so it is a product decision; (2) no PIN lockout threshold/window behind the documented 429, which trades a security control against an attendant locked out mid-shift; (3) session listing is only IMPLIED by FR-64 - if it is an audit surface it belongs in Epic 10 and 4.8's scope changes.
- (event) Auth contract committed 335ce2f. 36 tests pass, 16/17 negative controls red on demand (Dart half of AD-14 still SDK-blocked), all gates green except the by-design Dart one. No auth handler exists yet - every /auth operation answers 501 with its owning story.
- (finding by user) Tanim asked which auth API a tenant/property administrator and a Jazzware super admin use. It exposed a GAP: the contract covered SSO and PIN/badge only, but the console UX spine says 'SSO first, PASSWORD FALLBACK, property picker' and FR-1 has a Jazzware operator create the first administrator with NO identity connection - so the first tenant admin must sign in before SSO exists. SSO can never be the only way in.
- (decision) Added four operations under x-story 1.3 (d045c59): POST /auth/sign-in, /auth/credential/set-up, /auth/password/forgot, /auth/password/reset. Three credential types now, and the capability limit stays on the PIN specifically - a password administrator holds their full role. Emailed link tokens travel in a URL FRAGMENT, never a query string (no access log, no Referer), honouring the standing 'never a token in a URL' constraint. set-up returns a session; reset returns 204 and revokes every other session; forgot always 202 to avoid an enumeration oracle.
- (finding) OPEN, must be settled in epics.md before building: credential recovery policy (self-service reset vs time-boxed Jazzware support request) and whether the password fallback requires MFA - NFR-7 is silent. forgot/reset are in the contract at SHAPE level only. Also DG-4: the UX states the region at sign-in and Session carries no region field.
- (finding) JAZZWARE OPERATOR AUTH HAS NO OWNER. Correctly absent from the cell contract (FR-1: internal surface the product does not link to; AD-4: control plane outside the cells), but Story 1.1 AC-1 states 'Given I am authenticated as a Jazzware operator' as a PRECONDITION no story builds. Needs its own control-plane OpenAPI document and a new story - a change to raise in epics.md. Do NOT add an operator credential to a cell; that would defeat FR-1's 'no standing access'.
- (finding) Also flagged: 1.1 issues the first administrator's invitation and 1.3 redeems it, so between them a provisioned Tenant has an administrator who cannot sign in. The two must agree the token shape before either starts - same arrangement as 4.1/4.3 over the offline queue.
- (decision by user) Tanim's three decisions: (1) self-service password reset IS permitted; (2) complete the epic so 'Given I am authenticated as a Jazzware operator' has an owner; (3) MFA off by default, enabled from Settings, options OTP-email / Google Authenticator / Microsoft Authenticator. Answered follow-ups: Tenant-wide MFA enforcement YES; operator MFA same opt-in as tenant users (not mandatory in code); release = operator R1, MFA R2.
- (decision) Amended TWO status:final documents (2afa862), each with an amendment note. PRD: FR-84 per-user MFA, FR-85 Tenant-wide enforcement, FR-86 Jazzware operator authentication; release slicing updated. epics.md: Epic 11 (operator surface, R1, 3 stories) and Epic 12 (account security, R2, 4 stories). Every derived number RECOMPUTED: 12 epics, 94 stories, 86 FRs each owned exactly once, 94/94 sprint-status keys.
- (decision) contracts/control-plane-openapi.yaml created as a SECOND document: own servers prefix, own operatorBearerAuth scheme with a different issuer and audience, so an operator token is structurally unusable against a cell rather than merely unauthorised. POST /tenants and /tenants/{id}/support-access live there under x-story 1.1. Separation is GATED: drift gate fails on shared paths, an operator path in the cell doc, a cell path in the control-plane doc, or a shared security scheme; smoke asserts the cell serves none of it (404, not 501). Controls 18-19 prove it.
- (finding) MFA is TWO mechanisms, THREE labels: Google Authenticator and Microsoft Authenticator consume the same TOTP secret (RFC 6238), so they are labels on one method plus an appHint kept for support only. Told Tanim plainly since he asked for three options. Scope is the password credential only - provider-governed identities keep their own policy (FR-3) and PIN/badge on a Shared Device is out (five-second sign-in, FR-4/NFR-5). POST /auth/sign-in returns a discriminated SignInResult from 1.3 onwards so 12.2 breaks no shipped client.
- (finding) TWO GAPS raised rather than improvised: (1) EXPERIENCE-WEB.md (final) has NO per-user account surface among its 39, yet FR-84 needs Settings to belong to the individual - recorded in Story 12.1 as a change to raise in the UX spine; (2) Story 11.3 AC-3 needs one support-access grant in both the operator audit trail and the Tenant's own, a write across the AD-4 boundary no single transaction spans - named as an outbox/reconciliation decision. Also open: operator MFA is policy not code, so it needs an owner in Jazzware's security posture.
- (event) Implemented Stories 11.1 and 1.1 (9d194c8), both at status review. 1.1 required 11.1 first. 60 tests, 7 gates green bar the Dart one, 22/23 negative controls red on demand.
- (decision) Cell/control-plane separation enforced by DATABASE PRIVILEGES: jt_control has no grants in the cell schema, so 'SELECT FROM cell.events' as that role answers permission denied. Three independent separations - different signing secret, different token AUDIENCE checked per request, different DB role. Control 23 sets both secrets alike to prove the audience check alone still refuses an operator token at the cell.
- (decision) The operator NEVER sees the invitation token: only a hash is stored, the token goes to control_plane.outbox on which jt_control holds INSERT and nothing else. Consequence flagged: delivery needs the AD-8 notification adapter, which does not exist, so Story 1.3 cannot be exercised end to end until it does.
- (finding) THREE DEFECTS FOUND AND FIXED, all mine: (1) the control-plane contract required a region at Tenant creation, but FR-83/DG-4 put region on the PROPERTY and FR-1 creates no Properties; (2) deactivation answered 400 where the contract says 409 and returned a two-field object where it says Tenant - the drift gate proves BINDINGS match the document, nothing proved a RUNNING operation did, now asserted from the compiled schema's required fields; (3) a second ValidationError class in core/tenant would have turned a 400 into a 500 because the cell's handler tests identity not shape.
- (finding) TWO DEVIATIONS RAISED for Tanim: (1) the internal surface shares a process and origin with the cell - Story 1.1's structure notes say 'no separate deployable' so it is the /control/v1 namespace, but AD-4 puts the control plane outside the cells and one image with a second entrypoint is cheap now, awkward later; (2) the bootstrap operator seed is 11.2's AC-2 taken early, because sign-in cannot be exercised without an account. Also NOT built: 11.1 AC-5, the W35 amber-accent internal console surface.
- (event) Story 1.2 implemented (a5d2fe2), status review. 92 tests, 7 gates green bar the Dart one, 27/28 negative controls red on demand.
- (decision) TenantScope is its OWN type and Scope extends it, rather than making propertyId optional. Creating the first Property is the one operation AD-3 cannot cover; this way a Tenant-scoped principal is not assignable where a Scope is required, withTenantScope pins only the tenant, and cell RLS (which needs both settings) returns nothing. resolvePrincipal still demands both. Control 28 proves it.
- (decision) control_plane.cells added and a cell REGISTERS ITSELF each deploy from CELL_NAME/CELL_REGION, so 'the Property exists in the chosen region's cell' is checkable: a region no active cell serves is refused at creation with the available ones named. Cross-cell provisioning NOT built - one cell deployed means its region works and others are refused, which keeps the multi-region gap visible.
- (decision) Inheritance by reference proved end to end: property_settings links to tenant_settings.version plus an override set, no copied values. Change a Tenant default -> inheriting Property sees it; override -> it stops; change again -> no re-inherit. Story 1.6 depends on exactly this. Region immutability enforced in three places (aggregate, no route, DB trigger) and AC-3's write-refusal sits at the tenancy boundary so 3.1 and 2.1 inherit it.
- (finding) Running it exposed three things reading would not: jt_app had no INSERT on control_plane.events (migration 007); the Story 1.0 fixture Tenants had no settings row so a Property could not be created under them (migration 006); the region trigger refused NULL->cell, blocking initial placement of fixture Properties. Three new migrations rather than editing applied 005. Also my own bug: a basename collision in staging overwrote the API test with the unit test - caught only by cross-machine hash comparison.
- (event) Story 1.3 implemented, status review. Six designed-ahead auth operations flipped to built (unbuilt 20 -> 14) plus three new ones the story needed: POST /staff, GET /staff, GET /roles. Migration 008 adds staff_members, staff_roles, staff_credentials, sessions, password_resets. 24 new unit tests (61 total), a new boundary suite, six new negative controls (34).
- (decision) THE 1.1/1.3 TOKEN AGREEMENT, settled as the story required. Provisioning records an invitation with an email and nothing else - a Jazzware operator has no business typing a customer administrator's name or choosing their language - so CredentialSetUpRequest now requires name and languageTag, and redemption is where the person describes themselves. Required for EVERY redemption, not only the first-administrator case: one code path, no leak, and their own spelling beats what an administrator typed. Redemption creates the Staff Member when the invitation has none, with a TENANT-WIDE property_administrator grant. Until both halves existed a provisioned Tenant had an administrator who could not sign in; the suite now starts from provisioning so the seam itself is tested.
- (decision) AD-11 as code (ADR 0003): resolvePermissions in core/src/staff/roles.ts is pure with no I/O, edge/src/authorise.ts is its only caller, GET /auth/session serves its output to the interface, and every gated route reaches it through one gate() helper - so what the console renders and what the server enforces are the SAME array. Grants are read per request for the token's Property, which makes AC-3's re-resolution structural rather than remembered. FR-4 sits on the CREDENTIAL: each permission declares a class (operational/configuration/reporting), each credential type declares which classes it carries, pin and badge carry operational only, and a permission with no class is a compile error. Tested at the boundary by constructing the session Story 4.1 will produce - a real session row with credential_type pin held by a full administrator.
- (decision) TWO decisions beyond the story. (1) A session may be TENANT-scoped: Session.propertyId and region are optional because FR-1 creates an administrator before any Property exists - the same single AD-3 exception POST /properties already was. A Property-scoped route answers 403 naming the property picker, not 401, because telling an authenticated person their credential was rejected sends them hunting a token that was never the problem. (2) property.create and property.deactivate require TENANT-WIDE authority: the Harbour's administrator has no business creating or retiring a Property elsewhere in the estate, and AC-4 wants that refused server-side rather than hidden. Roles assignable Tenant-wide are a domain rule (property_administrator, corporate_viewer), not a column - staff_roles.property_id is simply nullable.
- (finding) DEAD CODE A GATE WAS WATCHING, found by rewiring rather than by reading. resolvePrincipal and resolveTenantPrincipal were left unused once the server switched to resolveCellPrincipal - and negative controls 23 and 28 POINTED AT THEM, so both gates would have kept passing while testing functions nothing served. False assurance is worse than no gate. Deleted both, rewired 23 (now sets all THREE signing secrets alike, against the live resolver) and 28 (breaks the server's own Property demand, patching two lines so the request proceeds instead of crashing - a control that goes red because the process threw proves less). Story 1.3's change also moved the Tenant-scope refusal from 401 to 403, so the isolation gate's expectation was widened AND strengthened in the same edit: it now asserts no Property data comes back either way, which is the guarantee the status was only standing in for.
- (finding) FLAGGED, NOT WORKED AROUND. (1) The AD-8 notification adapter still does not exist, so set-up and reset links reach control_plane.outbox and go no further - an R1 RELEASE BLOCKER, since today a real administrator cannot finish signing up without somebody reading a database table. (2) An email address is unique WITHIN a Tenant and deliberately not across them: global uniqueness would make an invitation's 409 reveal an account at another Tenant, the exact leak FR-1 exists to prevent. So sign-in resolves an address against every Tenant and needs exactly one password to match; the same address and password at two Tenants is unresolvable, refused generically and logged. Fix is a Tenant hint, and the slug that carries it arrives with SSO in 1.5. (3) Rate limiting is an in-memory fixed window, so two replicas double the limit - real, and not dressed up as more; the durable version belongs with the still-unowned PIN lockout policy. (4) PIN sign-in needs a way to resolve a person: the assumption left for 4.1 is a staff picker then a PIN. All four are ADR 0002 questions 7-10. Also: self-service reset with no second factor means every password account is a mailbox away from takeover until Epic 12 - now written at the endpoint, not only in the ADR.
- (decision) npm run refresh now runs the SUITE AND THE GATES as a fifth step and prints its own TEST RESULT line - reported, never fatal, so a suite failing for an environment reason cannot make 'did the containers rebuild' unanswerable. It also asserts the CONVERSE of the 501 check: the six operations Story 1.3 flipped must no longer answer 501, so a flag flipped without a handler behind it shows up in the refresh log rather than in a review. Added SESSION_TOKEN_SECRET (the third signing secret, own audience, fails closed at boot as its own call in main.ts - folding the checks together once masked three of four failure cases) and DATABASE_URL_CONTROL to the local .env.
- (finding) VERIFICATION BY EXECUTION, in a way that was not available before: the bridge VM has no Docker and no Postgres, so the source tree was mirrored into the cloud container and all eight migrations applied to a real Postgres 16 from scratch. 136/136 tests and 33/35 controls red-verified there (1 unverifiable - no Dart SDK, 1 skipped - no console deps). NOT a substitute for npm run refresh on the Mac, since two earlier defects lived exactly in that gap, but it found three things reading did not. (1) GET /v1/staff?propertyId= LEAKED WIDER THAN ASKED: a Tenant-wide caller has no per-Property authority list so the check could not fire, and the query's property_id IS NULL branch returned every Tenant-wide Staff Member for a filter naming ANOTHER Tenant's Property - isolation held, the answer did not, which is what that function's own comment promised could not happen. (2) I had quietly reversed a documented boundary by granting jt_app SELECT on control_plane.invitations, which migration 004 revoked on purpose and provisioning.test.ts asserts; the cell now holds no privilege on that table and gets three SECURITY DEFINER functions instead - lookup by token hash with a row lock (single-use becomes a database property), a redemption that refuses a second attempt, and an issue function with a hard-coded staff_member scope so a cell can never mint a tenant_administrator invitation. Stronger than before, and control 35 proves it. (3) An eager assertion message consumed a fetch body, reporting a broken test instead of the failure it was meant to explain.
- (finding) THREE DEAD GATES IN ONE STORY, all found by rewiring or running rather than by reading: controls 23 and 28 were pointed at resolvePrincipal/resolveTenantPrincipal, which the server had stopped calling; control 14 flipped x-story 1.3's x-implemented: false and Story 1.3 built all six of those operations, so its patch matched nothing and it passed while testing nothing. Control 14 is now story-agnostic and reports UNVERIFIED once the last flag flips rather than passing silently. A gate is only as good as the last time somebody checked what it was pointed at - and a gate that has stopped biting is worse than no gate, because it is counted.
- (event) Housekeeping: _to_delete/ (19 git lock files from the bridge's delete-permission workaround) was tracked in git. Untracked and gitignored, along with *.tgz.
