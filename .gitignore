node_modules/
dist/
.env
*.log
.dart_tool/
clients/console/dist/

# Scratch delivery archives. Ten of these were tracked and were purged from
# history before the first push; ignoring the pattern is what stops it recurring.
*.tgz
*.tar
*.bundle

# The bridge cannot delete files on the Mac without a per-session grant, so things
# to be removed are parked here. Never committed - 19 git lock files were, once.
_to_delete/

# Where stale git lock files are parked when the bridge cannot delete them.
.scratch/
